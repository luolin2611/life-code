server:
  port: 10004
  servlet:
    context-path: /record-account #根路径

spring:
  application:
    name: eureka-client-record-account
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver #mysql驱动
    url: jdbc:mysql://localhost:3306/record_account
    username: root
    password: root123456

#logging:
#  level:
#    cn.lifecode.music: debug

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10001/eureka/